spring:
  application:
    name: badge
  data:
    mongodb:
      database: odoru
      username: root
      password: admin
      authentication-database: admin
server:
  port: 8082
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8083/eureka/
